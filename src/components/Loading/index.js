import React from 'react';
import './Loading.css';

const Loading = () => (
  <span className="Loading">
    <p>Loading</p>
    <span className="Loading-Dot"/>
    <span className="Loading-Dot"/>
    <span className="Loading-Dot"/>
  </span>
);

export default Loading;